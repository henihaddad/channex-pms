# Architecture decision records

One file per decision, numbered, never edited after acceptance (a new ADR supersedes an old one).
Format: context, decision, consequences. Referenced from the specs in `docs/specs/`.
