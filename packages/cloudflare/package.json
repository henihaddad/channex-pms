{
  "name": "@pms/cloudflare",
  "version": "0.0.0",
  "private": true,
  "description": "Cloudflare Workers glue shared by apps/web and apps/worker-cf: queue bindings, the outbox publisher over Queues, the message envelope.",
  "license": "SEE LICENSE IN ../../LICENSE.md",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint src",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@pms/core": "workspace:*",
    "@pms/db": "workspace:*",
    "@pms/jobs": "workspace:*",
    "@pms/runtime": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22",
    "eslint": "^9",
    "typescript": "^5",
    "vitest": "^4.1.11"
  }
}
