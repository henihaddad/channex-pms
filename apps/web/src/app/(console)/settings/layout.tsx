import type React from "react";

/** Settings is one section; its pages are listed under it in the sidebar. */
export default function SettingsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
